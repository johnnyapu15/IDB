UPDATE ORDERPROD
SET (PROD_ID = :PROD_ID_MOD), (ORD_REQUEST_QUANTITY = :ORD_REQUEST_QUANTITY)
    ,(ORD_RESULT_QUANTITY = :ORD_RESULT_QUANTITY), (ORD_COST = :ORD_COST)
WHERE (ORD_ID = :ORD_ID) AND (PROD_ID = :PROD_ID);