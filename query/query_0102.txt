INSERT INTO ORDERPROD
(ORD_DAT, ORD_ID, PROD_ID, ORD_REQUEST_QUANTITY, DEPOSIT_DAT, ORD_COST)
VALUES (:ORD_DAT, :ORD_ID, :PROD_ID, :ORD_REQUEST_QUANTITY, :DEPOSIT_DAT,
(:ORD_REQUEST_QUANTITY* 
(SELECT SELL_PRICE
FROM PROD
WHERE (PROD_ID = :PROD_ID))));
